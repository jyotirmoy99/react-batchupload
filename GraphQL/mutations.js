/*import { gql } from "@apollo/client";

export const SAMPLE_MUTATION = gql`
  mutation createPost(
    $input: CreatePostInput!
  ) {
    createPost(input: $input) {
    id
    title
    body
  }
  }
`;
*/
